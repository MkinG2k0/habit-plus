---
phase: 01-quick-logging-core-local-persistence
plan: 03
type: execute
wave: 3
depends_on:
  - 01-PLAN-02
files_modified:
  - src/features/exercise/ui/useQuickLogSetForm.ts
  - src/features/exercise/ui/ExerciseBody.tsx
  - src/features/exercise/ui/ExerciseBody.test.tsx
  - src/features/exercise/index.ts
autonomous: false
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
must_haves:
  truths:
    - "Пользователь добавляет подход за 1-2 действия и сразу видит его в списке."
    - "Пользователь редактирует/удаляет подход в текущем экране без потери контекста."
    - "Пользователь видит подпись `Предыдущий: {вес} кг × {повторы}` рядом с вводом, если есть история."
  artifacts:
    - path: "src/features/exercise/ui/useQuickLogSetForm.ts"
      provides: "Хук формы и обработчиков для weight/reps без бизнес-логики в JSX"
    - path: "src/features/exercise/ui/ExerciseBody.tsx"
      provides: "UI quick-log потока с copy из UI-SPEC и error/suggestion состояниями"
    - path: "src/features/exercise/ui/ExerciseBody.test.tsx"
      provides: "Интеграционные тесты 1-2 действия, edit/delete, suggestion rendering"
  key_links:
    - from: "ExerciseBody"
      to: "useCalendarStore storageError"
      via: "error banner rendering"
      pattern: "UI-SPEC error copy shown on storage failure"
    - from: "ExerciseBody"
      to: "getPreviousSetSuggestion result"
      via: "inline label rendering"
      pattern: "Предыдущий: {weight} кг × {reps}"
---

<objective>
Реализовать UI-контур быстрой записи подходов с точным контрактом copy/состояний из UI-SPEC для финального закрытия LOG-01..04.

Purpose: Доставить пользовательский сценарий quick-log end-to-end поверх уже устойчивой доменной логики.
Output: Обновленный `ExerciseBody` + выделенный hook формы + UI-интеграционные тесты.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-quick-logging-core-local-persistence/01-UI-SPEC.md
@.planning/phases/01-quick-logging-core-local-persistence/01-VALIDATION.md
@src/features/exercise/ui/ExerciseBody.tsx
@src/entities/calendarDay/slice/calendarStore.ts
@src/shared/ui/shadCNComponents/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Вынести логику формы set в отдельный hook</name>
  <files>src/features/exercise/ui/useQuickLogSetForm.ts, src/features/exercise/index.ts</files>
  <read_first>src/features/exercise/ui/ExerciseBody.tsx, .planning/phases/01-quick-logging-core-local-persistence/01-RESEARCH.md</read_first>
  <action>Создать hook `useQuickLogSetForm` с API `{ handleSetValueChange, handleAddSet, handleDeleteSet, getInputValue }`; инкапсулировать ограничения ввода (0..999, максимум 3 символа), типизированные обработчики (`ChangeEvent<HTMLInputElement>`), и переиспользуемые memoized callbacks для React-дерева; экспортировать hook через `src/features/exercise/index.ts`.</action>
  <acceptance_criteria>В `ExerciseBody` больше нет inline бизнес-логики в JSX для обработки `weight/reps`; все обработчики приходят из `useQuickLogSetForm`; сигнатуры без `any`.</acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/features/exercise/ui/ExerciseBody.test.tsx --reporter=basic --passWithNoTests</automated>
  </verify>
  <done>UI-компонент готов к чистой интеграции с состояниями suggestion/error без разрастания ответственности.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Обновить ExerciseBody по UI-SPEC и требованиям LOG</name>
  <files>src/features/exercise/ui/ExerciseBody.tsx</files>
  <read_first>.planning/phases/01-quick-logging-core-local-persistence/01-UI-SPEC.md, src/features/exercise/ui/ExerciseBody.tsx, src/entities/calendarDay/slice/calendarStore.ts</read_first>
  <behavior>
    - LOG-01: кнопка `Добавить подход` добавляет строку set максимум за 1-2 действия.
    - LOG-03: удаление вызывает подтверждение с текстом `Удалить подход? Действие нельзя отменить.`
    - LOG-04: при наличии истории отображается `Предыдущий: {вес} кг × {повторы}`.
  </behavior>
  <action>Переписать `ExerciseBody.tsx` на Tailwind/shadcn без CSS Modules в изменяемом компоненте; применить тексты из UI-SPEC: primary CTA `Добавить подход`, empty state `Подходов пока нет`, error copy `Не удалось сохранить подход локально...`; подключить `storageError` из store и рендерить non-blocking error-banner; реализовать confirm-поток удаления подхода через shadcn `Dialog` или `window.confirm` с точным текстом из UI-SPEC.</action>
  <acceptance_criteria>В компоненте нет новых `.module.css` зависимостей; визуальный/текстовый контракт UI-SPEC соблюден; состояния empty/suggestion/error доступны в одном экране.</acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/features/exercise/ui/ExerciseBody.test.tsx --reporter=basic</automated>
  </verify>
  <done>UI quick-log полностью отражает UX-контракт фазы и использует готовый доменный API без обходов.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Проверка UX-критерия 1-2 действия и читаемости состояний</name>
  <files>src/features/exercise/ui/ExerciseBody.tsx, src/features/exercise/ui/useQuickLogSetForm.ts</files>
  <read_first>.planning/phases/01-quick-logging-core-local-persistence/01-UI-SPEC.md, src/features/exercise/ui/ExerciseBody.tsx</read_first>
  <action>Запустить приложение в dev-режиме, пройти сценарий quick-log на реальном экране и зафиксировать соответствие шагов требованиям LOG-01..LOG-04 до разблокировки следующей работы.</action>
  <what-built>Автоматизированная реализация quick-log экрана с add/edit/delete/suggestion/error состояниями</what-built>
  <how-to-verify>
    1) Открыть экран упражнений и нажать `Добавить подход`.
    2) Убедиться, что новый set добавляется максимум за 1-2 действия.
    3) Изменить `повторы/вес`, перезагрузить страницу, проверить сохранение.
    4) Удалить подход и подтвердить диалог; проверить, что удален только выбранный.
    5) Проверить отображение строки `Предыдущий: ...` при повторном логировании упражнения.
  </how-to-verify>
  <acceptance_criteria>Пользователь подтверждает выполнение всех пяти шагов без функциональных расхождений и без визуальных блокеров.</acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/features/exercise/ui/ExerciseBody.test.tsx --reporter=basic</automated>
  </verify>
  <resume-signal>Ответьте `approved`, если UX принят, или перечислите отклонения.</resume-signal>
  <done>Подтверждено, что быстрый лог в реальном UI соответствует критериям LOG-01..04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User interaction -> UI handlers | Некорректный ввод и destructive-действия инициируются пользователем |
| UI rendering -> domain data | Исторические значения и storage errors переходят из store в интерфейс |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | set input fields (`weight`, `reps`) | medium | mitigate | Валидация на уровне hook/store, ограничение диапазона и типа, тесты на invalid input |
| T-01-08 | Elevation of Privilege | destructive delete action | medium | mitigate | Обязательное подтверждение удаления перед `deleteSet` |
| T-01-09 | Denial of Service | UI при `storageError` | high | mitigate | Неблокирующий error banner + сохранение работоспособности add/edit/delete |
</threat_model>

<verification>
`pnpm vitest run src/features/exercise/ui/ExerciseBody.test.tsx --reporter=basic`  
`pnpm lint`
</verification>

<success_criteria>
- LOG-01..04 подтверждены тестами и human-verify checkpoint.
- В UI соблюдены тексты и состояния из `01-UI-SPEC.md`.
- Компонент не содержит бизнес-логики в JSX и готов к дальнейшему расширению без God-component эффекта.
</success_criteria>

<output>
After completion, create `.planning/phases/01-quick-logging-core-local-persistence/01-03-SUMMARY.md`
</output>
