---
phase: quick-260422-q3a-createexercise
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/createExercise/ui/CreateExercise.tsx
autonomous: true
requirements:
  - QUICK-260422-Q3A
must_haves:
  truths:
    - "Пользователь может создать упражнение с тем же UX и валидациями, что и до рефакторинга."
    - "Пользователь может редактировать и удалять упражнение без поведенческих изменений."
    - "Работа с фото (камера/галерея/ограничения) сохраняет текущее поведение и ошибки."
  artifacts:
    - path: "src/features/createExercise/ui/CreateExercise.tsx"
      provides: "Более читаемая структура компонента с выделенными секциями и утилитными обработчиками"
  key_links:
    - from: "CreateExercise state"
      to: "save/update/delete handlers"
      via: "общие helper-функции и единая нормализация данных"
    - from: "photo actions"
      to: "photoDataUrls + photoError"
      via: "унифицированная обработка выбора/валидации фото"
---

<objective>
Сделать рефакторинг `CreateExercise` для повышения читаемости и поддерживаемости без изменения пользовательского поведения.

Purpose: упростить навигацию по коду и локализовать ответственность обработчиков.
Output: обновленный `CreateExercise.tsx` с более чистой структурой, меньшим дублированием и понятными зонами ответственности.
</objective>

<context>
@.planning/STATE.md
@src/features/createExercise/ui/CreateExercise.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Нормализовать состояние и lifecycle-инициализацию формы</name>
  <files>src/features/createExercise/ui/CreateExercise.tsx</files>
  <action>Вынести повторяющиеся начальные значения формы и сброс ошибок в локальные helper-функции (например, `buildInitialExerciseState`, `resetFormState`) и использовать их в `useEffect`/`handleClose`. Сохранить текущую логику для create/edit режимов и существующие значения по умолчанию (иконка, категория, описание, фото).</action>
  <verify>
    <automated>pnpm exec eslint src/features/createExercise/ui/CreateExercise.tsx</automated>
  </verify>
  <done>Инициализация/сброс формы описаны централизованно, поведение открытия/закрытия модалки не изменилось.</done>
</task>

<task type="auto">
  <name>Task 2: Упростить обработчики сохранения и фото-операций</name>
  <files>src/features/createExercise/ui/CreateExercise.tsx</files>
  <action>Разделить крупные обработчики на небольшие именованные функции: проверка дубликатов, подготовка payload для create/update, единая нормализация `name/description/photoDataUrls`, обработка ограничений фото и ошибок. Устранить дублирование между `handlePickPhotos` и `handlePickFromGallery`, сохранив текущие тексты ошибок и лимиты (10 МБ, до 8 фото).</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Код обработчиков разбит на читаемые блоки, все текущие правила валидации и тексты ошибок сохранены.</done>
</task>

<task type="auto">
  <name>Task 3: Привести JSX-структуру к секционной композиции внутри файла</name>
  <files>src/features/createExercise/ui/CreateExercise.tsx</files>
  <action>Сгруппировать разметку по секциям (название, описание, фото, категория, иконка, футер) через локальные render-функции или компактные inline-компоненты в этом же файле. Избавиться от длинных вложенных блоков, сохранив текущие классы, aria-атрибуты, подписи кнопок и порядок элементов.</action>
  <verify>
    <automated>pnpm run build</automated>
  </verify>
  <done>JSX стал проще для чтения, структура компонента секционирована, UI и поведение визуально/функционально эквивалентны текущим.</done>
</task>

</tasks>

<verification>
- `pnpm exec eslint src/features/createExercise/ui/CreateExercise.tsx`
- `pnpm exec tsc --noEmit`
- `pnpm run build`
</verification>

<success_criteria>
- Поведение `CreateExercise` полностью сохранено для create/edit/delete сценариев.
- Логика инициализации, валидации и фото-обработки декомпозирована и не дублируется.
- Компонент остается в одном файле, но имеет явные, читаемые секции и короткие обработчики.
</success_criteria>
