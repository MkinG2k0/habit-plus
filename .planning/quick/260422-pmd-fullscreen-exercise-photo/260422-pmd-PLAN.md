# Quick Task 260422-pmd

## Goal

Открывать фото упражнения в полноэкранном режиме по клику в табе `Инфо` модалки статистики.

## Tasks

1. Добавить состояние выбранного фото для fullscreen-просмотра в `StatisticCard`.
2. Сделать кликабельными миниатюры в табе `Инфо`.
3. Показать выбранное фото в отдельной полноэкранной модалке и сбрасывать состояние при закрытии.

## Verify

- `pnpm exec tsc --noEmit`
