# Quick 260422-0w8 — Summary

## Сделано

- В `userStore` добавлены `workoutListShowDaySummary` (по умолчанию `true`) и `setWorkoutListShowDaySummary`.
- На странице настроек — карточка «Общая информация о тренировке» с чекбоксом «Показывать блок над списком упражнений».
- `ExerciseList` показывает `WorkoutSummaryCard` только при включённой настройке.
- Экспорт/импорт профиля в JSON (`appSettingsSectionRegistry`) учитывает новое поле.

## Проверка

- `pnpm run build` — успешно.
- `pnpm run lint` — падает на существующей ошибке в `button.tsx` (не связано с этой задачей).
