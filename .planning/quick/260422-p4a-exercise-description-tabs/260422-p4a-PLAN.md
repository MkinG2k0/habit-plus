# Quick Task 260422-p4a

## Goal

Добавить описание упражнения в каталог и показать его в модалке `Статистика упражнения` во вкладке `Инфо`, оставив график во вкладке `Статистика`.

## Tasks

1. Расширить модель каталога упражнений полем `description` и обеспечить обратную совместимость через нормализацию данных.
2. Добавить поле описания в форму создания/редактирования упражнения и сохранить его в `exerciseStore`.
3. Обновить модалку `Статистика упражнения`: добавить табы `Статистика` и `Инфо`, отображать описание упражнения во вкладке `Инфо`.

## Verify

- `pnpm exec tsc --noEmit`
- `pnpm run lint` (допускаются уже существующие вне задачи предупреждения/ошибки)
