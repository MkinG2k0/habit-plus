# Quick Task 260422-pg7

## Goal

Добавить поддержку нескольких фото для упражнения: загрузка в создании/редактировании и просмотр всех фото во вкладке `Инфо` в модалке статистики.

## Tasks

1. Перевести модель каталога упражнения с одного `photoDataUrl` на массив `photoDataUrls`.
2. Добавить обратную совместимость в нормализации persisted-данных (поддержка legacy-поля `photoDataUrl`).
3. Обновить форму `CreateExercise` для загрузки нескольких изображений, предпросмотра и удаления отдельных фото.
4. Обновить `StatisticCard` для показа галереи фото в табе `Инфо`.

## Verify

- `pnpm exec tsc --noEmit`
