---
phase: 01-quick-logging-core-local-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/test/setup.ts
  - src/shared/lib/storage.ts
  - src/shared/lib/storage.test.ts
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "Данные тренировок остаются доступными после перезапуска браузера."
    - "При ошибке локального сохранения пользователь получает явный сигнал об ошибке, а приложение не падает."
  artifacts:
    - path: "vitest.config.ts"
      provides: "Единая test-инфраструктура для автоматической проверки Phase 01"
    - path: "src/shared/lib/storage.ts"
      provides: "Безопасная обертка localStorage с отказоустойчивым чтением/записью"
    - path: "src/shared/lib/storage.test.ts"
      provides: "Автотесты на успешную персистентность и fallback-поведение"
  key_links:
    - from: "src/shared/lib/storage.ts"
      to: "window.localStorage"
      via: "safe read/write wrappers"
      pattern: "try/catch + fallback result"
    - from: "src/shared/lib/storage.test.ts"
      to: "src/shared/lib/storage.ts"
      via: "vitest assertions"
      pattern: "quota/security and invalid-json scenarios"
---

<objective>
Сформировать надежный foundation для локальной персистентности и автопроверок, чтобы Phase 01 исполнялась с быстрым feedback loop.

Purpose: Убрать риск тихой потери данных в localStorage и сделать DATA-01 проверяемым автоматикой.
Output: Готовая vitest-конфигурация и безопасный storage-слой с тестами.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-quick-logging-core-local-persistence/01-RESEARCH.md
@.planning/phases/01-quick-logging-core-local-persistence/01-UI-SPEC.md
@.planning/phases/01-quick-logging-core-local-persistence/01-VALIDATION.md
@src/shared/lib/storage.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Поднять тестовый контур Phase 01</name>
  <files>package.json, vitest.config.ts, src/test/setup.ts</files>
  <read_first>.planning/phases/01-quick-logging-core-local-persistence/01-VALIDATION.md, package.json</read_first>
  <action>Добавить dev-зависимости `vitest`, `@testing-library/react`, `@testing-library/dom`, `jsdom`; добавить script `test:run` со значением `vitest run --reporter=basic`; создать `vitest.config.ts` с `environment: "jsdom"`, `setupFiles: ["src/test/setup.ts"]`, `globals: true`; в `src/test/setup.ts` реализовать deterministic mock для `crypto.randomUUID` и утилиту очистки `localStorage` после каждого теста.</action>
  <acceptance_criteria>В `package.json` присутствует script `test:run`; `pnpm vitest run --reporter=basic --passWithNoTests` завершается кодом 0; setup-файл реально подключается (проверяется через простой smoke test).</acceptance_criteria>
  <verify>
    <automated>pnpm vitest run --reporter=basic --passWithNoTests</automated>
  </verify>
  <done>Тестовая инфраструктура готова и может исполнять юнит/интеграционные тесты для последующих планов.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ввести безопасные storage-операции для DATA-01</name>
  <files>src/shared/lib/storage.ts</files>
  <read_first>.planning/phases/01-quick-logging-core-local-persistence/01-RESEARCH.md, src/shared/lib/storage.ts</read_first>
  <behavior>
    - Чтение из поврежденного JSON возвращает пустой объект без exception.
    - Ошибка `localStorage.setItem` возвращает признак неуспеха и не рушит UI-поток.
    - Сериализация сохраняет только ключи целевого месяца формата `MM-YYYY`.
  </behavior>
  <action>Добавить в `storage.ts` типизированные функции `safeReadMonthStorage(monthKey: string): Record<string, CalendarDay>` и `safeWriteMonthStorage(monthKey: string, payload: Record<string, CalendarDay>): { ok: boolean; reason?: "quota" | "security" | "unknown" }`; переписать `getDaysFromLocalStorage` и `saveDaysToLocalStorage` на эти функции без прямого `JSON.parse(localStorage.getItem(...) ?? "{}")` в теле публичных API.</action>
  <acceptance_criteria>Все публичные функции storage работают без необработанных исключений; при битом JSON возвращается пустой объект; при ошибке записи возвращается `{ ok: false, reason: ... }`.</acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/shared/lib/storage.test.ts --reporter=basic</automated>
  </verify>
  <done>Storage-граница отказоустойчива и пригодна для интеграции с календарным store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Закрыть DATA-01 автотестами storage-слоя</name>
  <files>src/shared/lib/storage.test.ts</files>
  <read_first>src/shared/lib/storage.ts, .planning/phases/01-quick-logging-core-local-persistence/01-VALIDATION.md</read_first>
  <behavior>
    - При валидном payload чтение после записи возвращает тот же набор дней.
    - При поврежденном JSON чтение возвращает `{}`.
    - При mocked quota error запись возвращает `{ ok: false }`.
  </behavior>
  <action>Создать таблицу тест-кейсов для функций `safeReadMonthStorage/safeWriteMonthStorage/getDaysFromLocalStorage/saveDaysToLocalStorage`; покрыть минимум 5 сценариев: happy-path, invalid-json, empty storage, quota/security exception, merge prev/current/next month.</action>
  <acceptance_criteria>Файл `src/shared/lib/storage.test.ts` содержит минимум 5 тестов, и все проходят локально.</acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/shared/lib/storage.test.ts --reporter=basic</automated>
  </verify>
  <done>Поведение persistence формально зафиксировано тестами и готово к использованию в следующих планах.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser input -> storage utils | Непроверенные/поврежденные данные попадают в чтение/запись localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `src/shared/lib/storage.ts` parse path | high | mitigate | Безопасный JSON parse с fallback `{}` и unit-тесты invalid-json |
| T-01-02 | Denial of Service | `localStorage.setItem` quota/security | high | mitigate | try/catch на запись + `{ ok: false, reason }` контракт для верхнего слоя |
| T-01-03 | Repudiation | Persistence regressions без тестов | medium | mitigate | Vitest-инфраструктура и автотесты DATA-01 в CI-совместимом формате |
</threat_model>

<verification>
`pnpm vitest run --reporter=basic --passWithNoTests`  
`pnpm vitest run src/shared/lib/storage.test.ts --reporter=basic`
</verification>

<success_criteria>
- DATA-01 имеет автоматическое покрытие на чтение/запись/ошибки.
- Storage API больше не бросает необработанные исключения при invalid JSON и quota ошибках.
- Тестовый контур готов для планов 02 и 03 без дополнительной настройки.
</success_criteria>

<output>
After completion, create `.planning/phases/01-quick-logging-core-local-persistence/01-01-SUMMARY.md`
</output>
