# Quick 260422-0w8: отображение «Общая информация о тренировке»

## Задачи

1. **userStore** — поле `workoutListShowDaySummary` (по умолчанию `true`), сеттер, merge при rehydrate.
2. **UI настроек** — карточка с чекбоксом на `SettingsPage`, экспорт из `features/exercise`.
3. **ExerciseList** — рендер `WorkoutSummaryCard` только при включённой настройке.
4. **appSettingsSectionRegistry** — включить поле в экспорт/импорт профиля.

## Проверка

- `pnpm run lint` и `pnpm run build` без ошибок.
