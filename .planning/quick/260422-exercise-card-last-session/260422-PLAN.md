# Quick task 260422

## Goal

Добавить в настройках переключатель и в свёрнутой карточке упражнения строку с результатом прошлой тренировки с тем же упражнением.

## Tasks

1. Persist-флаг в `userStore` + экспорт/импорт профиля в `appSettingsSectionRegistry`.
2. Карточка настроек + рендер подсказки в `ExerciseCard` (только свёрнутое состояние, без дубля при раскрытии).

## Done

Реализовано в коммите кода.
