# Quick Task 260422-pdm — Summary

## Выполнено

- В модель каталожных упражнений добавлено поле `photoDataUrl` с обратной совместимостью в нормализации.
- В форме создания/редактирования упражнения добавлена загрузка фото (`image/*`), проверка типа/размера (до 2 МБ), предпросмотр и удаление фото.
- Фото прокинуто через flow редактирования упражнения и сохраняется в `exerciseStore`.
- Во вкладке `Инфо` модалки статистики добавлен показ фото упражнения.

## Проверка

- `pnpm exec tsc --noEmit` — успешно.
