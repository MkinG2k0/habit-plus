---
phase: 01-quick-logging-core-local-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 01-PLAN-01
files_modified:
  - src/entities/calendarDay/model/selectors/getPreviousSetSuggestion.ts
  - src/entities/calendarDay/model/selectors/getPreviousSetSuggestion.test.ts
  - src/entities/calendarDay/slice/calendarStore.ts
  - src/entities/calendarDay/slice/calendarStore.test.ts
  - src/entities/calendarDay/index.ts
  - src/shared/lib/storage.ts
autonomous: true
requirements:
  - LOG-02
  - LOG-03
  - LOG-04
  - DATA-01
must_haves:
  truths:
    - "Пользователь может добавлять несколько подходов к одному упражнению в одной тренировке."
    - "Пользователь может отредактировать и удалить любой подход без повреждения остальных данных."
    - "При повторном логировании того же упражнения отображается подсказка предыдущего подхода."
  artifacts:
    - path: "src/entities/calendarDay/slice/calendarStore.ts"
      provides: "Action-контракты add/edit/delete/persist для сетов"
    - path: "src/entities/calendarDay/model/selectors/getPreviousSetSuggestion.ts"
      provides: "Селектор предыдущих значений для LOG-04"
    - path: "src/entities/calendarDay/slice/calendarStore.test.ts"
      provides: "Юнит-покрытие CRUD и интеграции с безопасным storage"
  key_links:
    - from: "calendarStore addSetToExercise"
      to: "getPreviousSetSuggestion"
      via: "default payload for new set"
      pattern: "new set prefills by selector result"
    - from: "calendarStore mutations"
      to: "saveDaysToLocalStorage"
      via: "post-mutation persistence call"
      pattern: "every write path persists or returns storage error flag"
---

<objective>
Закрыть доменный контур quick-log: предсказуемые CRUD-операции по подходам, подсказка предыдущего значения и безопасная персистентность после каждой мутации.

Purpose: Реализовать LOG-02/03/04 и усилить DATA-01 в слое сущности без бизнес-логики в UI.
Output: Обновленный `calendarStore`, новый селектор предложений, покрытие тестами доменной логики.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-quick-logging-core-local-persistence/01-RESEARCH.md
@.planning/phases/01-quick-logging-core-local-persistence/01-UI-SPEC.md
@src/entities/calendarDay/slice/calendarStore.ts
@src/shared/lib/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Зафиксировать интерфейс подсказки предыдущего подхода</name>
  <files>src/entities/calendarDay/model/selectors/getPreviousSetSuggestion.ts, src/entities/calendarDay/index.ts</files>
  <read_first>src/entities/calendarDay/slice/calendarStore.ts, .planning/phases/01-quick-logging-core-local-persistence/01-UI-SPEC.md</read_first>
  <action>Создать экспортируемую функцию `getPreviousSetSuggestion(days: Record<string, CalendarDay>, exerciseName: string): { weight: number; reps: number } | null`; алгоритм: обход дат в обратном хронологическом порядке, выбор последнего непустого сета упражнения по `exercise.name`; вернуть `null`, если данных нет. Добавить named export в `src/entities/calendarDay/index.ts`.</action>
  <acceptance_criteria>Сигнатура селектора строго соответствует контракту; при отсутствии истории возвращается `null`; селектор доступен через Public API сущности.</acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/entities/calendarDay/model/selectors/getPreviousSetSuggestion.test.ts --reporter=basic</automated>
  </verify>
  <done>Контракт LOG-04 определен в отдельном переиспользуемом модуле сущности.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Обновить calendarStore для LOG-02/LOG-03 и безопасного сохранения</name>
  <files>src/entities/calendarDay/slice/calendarStore.ts, src/shared/lib/storage.ts</files>
  <read_first>src/entities/calendarDay/model/selectors/getPreviousSetSuggestion.ts, src/entities/calendarDay/slice/calendarStore.ts, src/shared/lib/storage.ts</read_first>
  <behavior>
    - `addSetToExercise` добавляет новый set с `crypto.randomUUID()` и подставляет suggestion при наличии.
    - `setExerciseValues` валидирует строковые значения в диапазоне 0..999 и не пишет `NaN`.
    - `deleteSet` удаляет только целевой подход и оставляет остальные неизменными.
  </behavior>
  <action>В `calendarStore` после каждой мутации (`addExercise`, `setExerciseValues`, `addSetToExercise`, `deleteSet`, `deleteExercise`) вызывать безопасное сохранение месяца через `saveDaysToLocalStorage`; расширить API store полем `storageError: null | "quota" | "security" | "unknown"` и обновлять его при неуспешной записи; в `addSetToExercise` использовать `getPreviousSetSuggestion` для новых упражнений per LOG-04 вместо копирования только из текущего `exercise.sets`.</action>
  <acceptance_criteria>Каждый write-action приводит к попытке персистентности; при ошибке записи выставляется `storageError`; операции CRUD не повреждают соседние упражнения/подходы.</acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/entities/calendarDay/slice/calendarStore.test.ts --reporter=basic</automated>
  </verify>
  <done>Store гарантирует консистентность set-операций и безопасное поведение при сбоях localStorage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Покрыть store и selector регрессионными тестами</name>
  <files>src/entities/calendarDay/model/selectors/getPreviousSetSuggestion.test.ts, src/entities/calendarDay/slice/calendarStore.test.ts</files>
  <read_first>src/entities/calendarDay/model/selectors/getPreviousSetSuggestion.ts, src/entities/calendarDay/slice/calendarStore.ts, .planning/phases/01-quick-logging-core-local-persistence/01-VALIDATION.md</read_first>
  <behavior>
    - LOG-02: последовательные addSet вызовы увеличивают длину массива и сохраняют данные.
    - LOG-03: edit/delete применяются к одному id без сайд-эффекта на другие set.
    - LOG-04: selector отдает последний исторический вес/повторы.
    - DATA-01: при моке ошибки записи store выставляет `storageError`.
  </behavior>
  <action>Добавить тесты на чистые сценарии и edge-кейсы: пустая история, несколько дат одного упражнения, удаление последнего подхода, неверный numeric input; использовать deterministic даты (`dayjs("2026-04-14")`) и mock `localStorage` из test setup.</action>
  <acceptance_criteria>Набор тестов покрывает все 4 требования в этом плане (LOG-02, LOG-03, LOG-04, DATA-01) и стабильно проходит повторный прогон.</acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/entities/calendarDay/model/selectors/getPreviousSetSuggestion.test.ts src/entities/calendarDay/slice/calendarStore.test.ts --reporter=basic</automated>
  </verify>
  <done>Доменная логика quick-log защищена от регрессий и готова для UI-интеграции.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI input -> calendarStore actions | Пользовательский ввод веса/повторов пересекает границу доменной модели |
| calendarStore -> storage layer | Мутации данных пересекают границу персистентности |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `setExerciseValues` numeric parsing | high | mitigate | Явная валидация диапазона `0..999`, блокировка `NaN`, тесты invalid input |
| T-01-05 | Denial of Service | write path через `saveDaysToLocalStorage` | high | mitigate | Контракт `storageError` + graceful failure без падения store |
| T-01-06 | Information Disclosure | selector historical traversal | low | accept | Локальное однопользовательское приложение, данные не покидают браузер |
</threat_model>

<verification>
`pnpm vitest run src/entities/calendarDay/model/selectors/getPreviousSetSuggestion.test.ts --reporter=basic`  
`pnpm vitest run src/entities/calendarDay/slice/calendarStore.test.ts --reporter=basic`
</verification>

<success_criteria>
- LOG-02/03/04 реализованы в entity-слое и покрыты автоматическими тестами.
- После каждой мутации стора выполняется безопасная попытка persistence.
- Для UI доступен единый сигнал `storageError` и готовый selector для `Предыдущий: ...`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-quick-logging-core-local-persistence/01-02-SUMMARY.md`
</output>
