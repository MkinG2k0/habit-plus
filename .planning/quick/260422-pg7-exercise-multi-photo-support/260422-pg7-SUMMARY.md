# Quick Task 260422-pg7 — Summary

## Выполнено

- Поле фото упражнения переведено на массив `photoDataUrls` во всех релевантных типах, сторе и UI-пэйлоадах редактирования.
- В нормализатор добавлена миграция legacy-формата: если есть старое строковое `photoDataUrl`, оно преобразуется в массив из одного элемента.
- В `CreateExercise` добавлена загрузка нескольких фото (`multiple`), чтение файлов, валидация типа/размера, предпросмотр и удаление отдельных/всех фото.
- Во вкладке `Инфо` `StatisticCard` добавлен показ всех фото упражнения в виде сетки.

## Проверка

- `pnpm exec tsc --noEmit` — успешно.
