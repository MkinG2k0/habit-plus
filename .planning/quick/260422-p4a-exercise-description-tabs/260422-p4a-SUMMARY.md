# Quick Task 260422-p4a — Summary

## Выполнено

- Добавлено поле `description` в каталожную модель упражнения (`CatalogExercise`) и в нормализацию persisted-данных.
- Обновлены сценарии создания/редактирования упражнения: новое поле `Описание выполнения` сохраняется в стор.
- Модалка `Статистика упражнения` получила табы `Статистика` и `Инфо`; во вкладке `Инфо` показывается описание упражнения.

## Проверка

- `pnpm exec tsc --noEmit` — успешно.
- `pnpm run lint` — есть существующая ошибка в `src/shared/ui/shadCNComponents/ui/button.tsx` и предупреждения вне скоупа этой задачи.
